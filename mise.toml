[tools]
task = "3"
kcl = "0.11.3"
crossplane = "2.1.3"
python = "3.13"
# `task schema` runs `flux schema extract crd` + `flux schema validate`. The
# schema plugin is installed separately (see .github/workflows/ci.yaml); mise
# only provides the flux binary it plugs into.
flux2 = "2.9.4"
