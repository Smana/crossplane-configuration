apiVersion: cloud.ogenki.io/v1alpha1
kind: GCPWorkloadIdentity
metadata:
  name: xplane-external-dns
  namespace: infrastructure
spec:
  serviceAccount:
    name: external-dns
  roles:
    # A pre-created custom role. Custom roles must exist in OpenTofu before a
    # claim can name one — see the module README.
    - "projects/ogenki-435905/roles/xplane_dns_editor"
    # A predefined role, to exercise both name shapes through the slugger.
    - "roles/monitoring.viewer"
